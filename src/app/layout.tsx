import type { Metadata } from "next";
import { Inter } from "next/font/google";
import "./globals.css";

const inter = Inter({ subsets: ["latin"] });

export const metadata: Metadata = {
  title: "Commercial Builder Practice Exam | Medium Rise Licensing",
  description: "Practice exam portal for Commercial Builder - Medium Rise building licensing in Victoria. 200 questions covering NCC, safety, and construction practices.",
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en">
      <body className={inter.className}>{children}</body>
    </html>
  );
}
